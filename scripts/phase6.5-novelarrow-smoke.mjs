import assert from 'node:assert/strict';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const {novelArrowAdapter:p}=require('../backend/dist/providers/novelarrow/adapter.js');
const items=await p.search('Shadow Slave');assert.ok(items.length);const item=items.find(x=>x.sourceId==='shadow-slave');assert.ok(item,'expected searched title');const d=await p.getDetails(item.sourceId);assert.ok(d.title);const chapters=await p.getChapters(item.sourceId);assert.ok(chapters.length);const content=await p.getNovelContent(item.sourceId,chapters[0].id);assert.ok(content.paragraphs.length>2);console.log('PASS NovelArrow search/details/chapters/text',chapters.length,'chapters',content.paragraphs.length,'paragraphs');
