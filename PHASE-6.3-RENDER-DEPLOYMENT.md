# Talos Phase 6.3 Render Deployment

## 1. Objective

Prepare and verify the Talos backend for Render deployment using the already-migrated Aiven MySQL database, without switching the Expo frontend or changing the production API URL.

## 2. Deployment Status

Render Dashboard/API access was not available in this environment. No Render Web Service was created or deployed, and no deployed URL exists to verify.

Status: **Preparation complete; manual Render deployment required**.

## 3. Render Service Configuration

Create a Render **Web Service** from the repository's `main` branch.

- Root directory: `backend`
- Runtime: Node
- Build command: `npm ci && npm run build:mysql`
- Start command: `npm start`
- Health-check path: `/health/ready`
- Service type: Web Service
- Render supplies `PORT`; do not hardcode it in Render.
- The backend defaults `HOST` to `0.0.0.0`.

The readiness endpoint checks the database with `SELECT 1` and returns HTTP 503 when the database is unavailable. It does not perform expensive external-provider checks, so it is appropriate for the Render health check.

## 4. Required Environment Variable Names

Configure these in Render's Environment settings. Do not put values in source control or this document:

- `NODE_ENV=production`
- `DATABASE_URL` set to the existing Aiven MySQL connection string
- `JWT_SECRET` set to a strong production secret
- `CORS_ORIGIN` set to the exact trusted HTTPS web origin(s), comma-separated if needed
- `JWT_EXPIRES_IN` set to the chosen token policy, currently `7d` is the local example
- `NOVEL_GATEWAY_URL` only if a separate compatible novel service is enabled
- `CONSUMET_BASE_URL` only if an authorized self-hosted Consumet service is enabled
- `SCRAPER_BACKEND_URL` only if a trusted scraper backend is enabled
- `PROVIDER_REQUEST_TIMEOUT_MS` if overriding the bounded default
- `ASURA_BASE_URL`, `ASURA_TIMEOUT`, and `ASURA_RATE_LIMIT_MS` only if the limited Asura adapter is later enabled

Do not manually set `PORT` unless Render requires an override; the application reads Render's assigned port.

## 5. Aiven MySQL Architecture

```text
Render Web Service
        |
        | DATABASE_URL from Render secret environment
        v
Prisma MySQL Client generated by build:mysql
        |
        v
Existing Aiven MySQL schema
```

The Aiven schema was already migrated and verified in Phase 6.2B. No migration or reset is required for this deployment. If a future migration is added, use the isolated MySQL migration artifact and `prisma migrate deploy` only after review; do not use `db push` or reset commands.

## 6. Prisma Deployment Architecture

Local development continues to use:

```text
backend/prisma/schema.prisma -> SQLite
```

Render uses:

```text
backend/prisma/mysql/schema.prisma -> MySQL
```

The `build:mysql` script generates `@prisma/client` from the MySQL schema and then runs TypeScript compilation. The runtime command remains the existing `node dist/server.js`; no provider or content-gateway code changes are needed.

## 7. Production Security Configuration

- Production startup rejects the development JWT fallback.
- Production startup rejects wildcard CORS.
- Helmet remains enabled.
- Auth-protected endpoints remain protected.
- Provider upstreams remain configured values; no arbitrary URL proxy was introduced.
- Secrets remain environment-only.
- Aiven credentials must be stored in Render's secret environment settings.

## 8. Provider Configuration

The provider registry and content gateway are unchanged. MangaDex and Narou remain available through their existing paths. `proxy-consumet` remains configuration-dependent, and anime is not promoted to verified playback merely because the backend deploys. Provider health remains available at `/api/providers/health`.

## 9. Local Production-Style Verification

The local Render simulation passed using the private Aiven environment:

- MySQL Prisma generation: PASS
- Backend TypeScript build: PASS
- Production-mode startup: PASS
- `/health`: PASS
- `/health/ready`: PASS with database reachable
- `/api/providers/health`: PASS
- `/api/auth/me` without credentials: HTTP 401 as expected
- `/api/content/providers`: HTTP 200

The normal SQLite Prisma Client was regenerated after the simulation.

## 10. Local SQLite Regression

Local SQLite remains the checked-in workflow. Phase 6.2B already verified SQLite validation, generation, migration status, backend build, health, readiness, provider health, Phase 3, Phase 4, and MangaDex regressions after Aiven verification. No frontend API URL was changed.

## 11. Deployment Result

- Render service: **NOT CREATED**
- Render deployment: **NOT RUN**
- Deployed HTTPS URL: **None**
- Aiven database: **Previously verified and migrated**
- Expo production URL: **Unchanged**

## 12. Manual Actions Required

1. In Render, create a Web Service from the repository and `main` branch.
2. Set root directory to `backend`.
3. Set build command to `npm ci && npm run build:mysql`.
4. Set start command to `npm start`.
5. Set health-check path to `/health/ready`.
6. Add the environment variable names above and the private values in Render's environment manager.
7. Deploy and inspect the build/start logs.
8. Verify the resulting HTTPS URL at `/health`, `/health/ready`, `/api/providers/health`, `/api/content/providers`, and unauthenticated `/api/auth/me`.

Do not change the Expo frontend API URL until these checks pass and Phase 6.4 begins.

## 13. Known Limitations

- Render deployment and public HTTPS verification are pending manual Dashboard access.
- The frontend typecheck retains the known unrelated `cursor/canvas` declaration issue.
- `git diff --check` retains the pre-existing trailing blank-line warning in `backend/src/providers/index.ts`.

No Phase 6.4 work was started.

**PHASE 6.3 PREPARATION COMPLETE — MANUAL RENDER DEPLOYMENT REQUIRED — WAITING FOR CONFIRMATION.**
